import 'package:flutter/material.dart';

void main() => runApp(const MySignIn());

class MySignIn extends StatelessWidget {
  const MySignIn({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Column(
          children: const <Widget>[

          ],
        ),
      ),
    );
  }
}
